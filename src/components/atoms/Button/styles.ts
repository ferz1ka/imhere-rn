import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  button: {
    width: 50,
    borderRadius: 8,
    backgroundColor: '#0cb466',
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonLabel: {
    fontSize: 24,
    color: '#FFF',
  }
})