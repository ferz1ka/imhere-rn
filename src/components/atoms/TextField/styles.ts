import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    flex: 1,
    borderRadius: 8,
    backgroundColor: '#1F1E25',
    color: '#FDFCFE',
    padding: 16,
    fontSize: 16,
  },
})