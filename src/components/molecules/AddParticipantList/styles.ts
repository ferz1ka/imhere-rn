import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    gap: 16,
    paddingBottom: 16,
  },
  title: {
    color: '#FDFCFE',
    fontSize: 14,
    fontWeight: 'bold',
  },
})